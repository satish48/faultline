[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "python -m ensurepip --upgrade",
  "python -m pip install --upgrade pip",
  "python -m pip install -r agentwatch/requirements.txt"
]

[start]
cmd = "python -m uvicorn agentwatch.api.main:app --host 0.0.0.0 --port $PORT"